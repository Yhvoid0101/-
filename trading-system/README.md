# TRAE Work CN Trading System

This is the unified, single active source root for future Codex optimization.

The runtime baseline comes from WSL `/home/lmy/hermes_v6/sandbox_trading`. C-drive legacy tools are included only when they are unique and clearly tool-shaped. TRAE context is distilled into safe documentation. D-drive is recorded as an incomplete mirror and excluded.

Use `manifests/four_sources_audit.json` to trace every inspected file and `manifests/selected_legacy_tools.json` to trace every C-drive addition.
